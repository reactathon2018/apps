import React from 'react';

import { Navbar, Nav, NavItem } from 'reactstrap';

const Footer = () => {
  return (
    <Navbar>
      <Nav navbar>
        <NavItem>
          {null}
        </NavItem>
      </Nav>
    </Navbar>
  );
};

export default Footer;
